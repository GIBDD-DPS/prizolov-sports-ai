meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
run:
  persistenceMount: /data
  image: null
  command: uvicorn app.main:app --host 0.0.0.0 --port 8080
  args: null
  containerPort: 8080
  servicePort: 80
