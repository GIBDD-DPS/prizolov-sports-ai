meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

run:
  # 👍 ИСПРАВЛЕНО: В PYTHONPATH прописаны все возможные пути вашей структуры проекта
  command: "PYTHONPATH=.:./app:./prizolov_sports_ai uvicorn app.main:app --host 0.0.0.0 --loop asyncio --workers 1"
