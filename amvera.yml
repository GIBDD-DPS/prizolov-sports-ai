meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

build:
  requirements: requirements.txt

run:
  # 👍 ИСПРАВЛЕНО: Полностью удален флаг --port. Amvera сама подставит порт по умолчанию.
  command: "PYTHONPATH=. uvicorn main:app --host 0.0.0.0 --loop asyncio --workers 1"
