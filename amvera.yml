meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

run:
  # Запускаем приложение через модуль app.main и слушаем порт платформы.
  command: "uvicorn app.main:app --host 0.0.0.0 --port $PORT --loop asyncio --workers 1"
