meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

run:
  # Мы убрали --port, теперь Amvera сама подставит верный порт при запуске
  command: "uvicorn main:app --host 0.0.0.0 --loop asyncio --workers 1"
