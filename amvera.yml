meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

run:
  # Запускаем напрямую наш корневой main.py и передаем динамический порт Amvera
  command: "uvicorn main:app --host 0.0.0.0 --port $PORT --loop asyncio --workers 1"
