meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

run:
  # 👍 ИСПРАВЛЕНО: Блок build полностью удален, пути PYTHONPATH настроены для папки app
  command: "PYTHONPATH=.:./app uvicorn app.main:app --host 0.0.0.0 --loop asyncio --workers 1"
