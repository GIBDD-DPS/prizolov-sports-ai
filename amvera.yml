meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

run:
  # Если PORT не задан платформой, используем 8080.
  command: "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080} --loop asyncio --workers 1'"
