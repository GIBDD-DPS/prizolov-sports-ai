meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

build:
  requirements: requirements.txt

run:
  # 👍 ИСПРАВЛЕНО: Указан правильный путь к модулю внутри подпапки app
  command: "PYTHONPATH=. uvicorn app.main:app --host 0.0.0.0 --loop asyncio --workers 1"
