# ============================================
# Copyright (c) 2026
# PRIZOLOV SPORTS AI v14.12 (STORE-FRONT OPTIMIZED)
# Author: Dm.Andreyanov
# Organization: Prizolov Market / Prizolov Lab
# ============================================
# Root requirements for Amvera python-pip builds.
# Docker build uses ЭТОТ файл (COPY requirements.txt . в Dockerfile указывает
# на корень репозитория) — backend/requirements.txt Docker'ом не используется,
# несмотря на более старый комментарий здесь же. Держи оба файла в синхроне
# вручную, пока это не объединено в один источник правды.

fastapi==0.110.0
uvicorn==0.29.0
sqlalchemy==2.0.25
psycopg2-binary==2.9.10
pydantic-settings==2.1.0
httpx==0.27.0
python-dotenv==1.0.0
jinja2==3.1.3
alembic==1.13.1

agenomics @ git+https://github.com/GIBDD-DPS/agenomics.git
